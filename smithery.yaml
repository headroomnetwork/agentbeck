startCommand:
  type: stdio
  config:
    command: "python"
    args:
      - "mcp_server.py"
    env:
      AGENTBECK_URL: "https://agentbeck.bot"
build:
  command: "pip install -r requirements.txt"
